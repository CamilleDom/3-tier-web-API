all:
  vars:
    ansible_user: admin
    ansible_ssh_private_key_file: /tmp/deploy_key  
    ansible_host_key_checking: no
  children:
    prod:
      hosts:
        camille.dommergue.takima.school:
          ansible_host: camille.dommergue.takima.school
