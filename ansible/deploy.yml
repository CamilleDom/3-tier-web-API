---
- hosts: all
  become: true
  gather_facts: true

  roles:
    - docker
    - create_network
    - launch_database
    - launch_app
    - launch_frontend
    - launch_proxy

  post_tasks:
    - name: Display deployment summary
      debug:
        msg: |
          ========================================
          Deployment Complete!
          ========================================
          Backend API: http://camille.dommergue.takima.school:8080
          HTTP Server: http://camille.dommergue.takima.school
          ========================================